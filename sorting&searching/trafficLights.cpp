#include <iostream>
#include <set>
#include <vector>
using namespace std;
bool comparator(vector<int> first, vector<int> second){

}
int main(){
    int positions;
    cin>>positions;
    int n;
    cin>>n;
    multiset<vector<int> > ms;
    set<int> s;
    s.insert(0);
    s.insert(positions);
    ms.insert({positions, 0});

}
